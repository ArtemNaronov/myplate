# MyPlateService - Помощник по рецептам и ежедневному меню

Полнофункциональное веб-приложение для управления рецептами, генерации ежедневных меню на основе калорий, времени приготовления и наличия ингредиентов, с интеграцией Telegram WebApp и полноценной системой авторизации.

## 🆕 Последние обновления

### Версия 1.3.0 (Декабрь 2025)

- ✅ **Переключение темы**: Добавлена возможность переключения между светлой и темной темой
  - Поддержка системной темы (автоматическое определение)
  - Сохранение выбора пользователя
  - Плавные переходы между темами
- ✅ **Улучшенная диаграмма БЖУ**: 
  - Сумма калорий вынесена из центра диаграммы под неё для лучшей читаемости
  - Исправлена видимость текста в темной теме
- ✅ **Улучшенный UI**: Кнопки в карточках теперь прижаты к низу для единообразного вида

### Версия 1.2.0 (Декабрь 2025)

- ✅ **Удаление меню**: Добавлена возможность удалять сохраненные дневные и недельные меню
- ✅ **Исправления недельных меню**: Исправлена проблема с сохранением и отображением недельных меню
- ✅ **Автоматическое заполнение кладовой**: При создании пользователя автоматически добавляются базовые продукты (41 продукт)
- ✅ **Оптимизация генерации меню**: Улучшена логика fallback при отсутствии подходящих рецептов
- ✅ **Улучшенная обработка времени**: Параметры времени приготовления игнорируются, если равны 0

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Возможности](#возможности)
- [Архитектура проекта](#архитектура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Установка и настройка](#установка-и-настройка)
- [API документация](#api-документация)
- [Структура базы данных](#структура-базы-данных)
- [Разработка](#разработка)
- [Деплоймент](#деплоймент)

## 🛠 Технологический стек

### Frontend
- **Next.js 14** (App Router, TypeScript)
- **TailwindCSS** + **shadcn/ui** для UI компонентов
- **next-themes** для переключения светлой/темной темы
- **React Server Components** для оптимизации производительности
- **Axios** для HTTP запросов
- **TypeScript** для типобезопасности

### Backend
- **Go 1.24** - современный язык программирования
- **Fiber v2** - быстрый веб-фреймворк
- **PostgreSQL 15** - реляционная база данных
- **JWT** - аутентификация через токены
- **bcrypt** - хеширование паролей
- **lib/pq** - драйвер PostgreSQL

### Infrastructure
- **Docker** & **Docker Compose** - контейнеризация
- **PostgreSQL** - основная база данных
- **PgAdmin** (опционально) - веб-интерфейс для управления БД

## ✨ Возможности

### Управление рецептами
- Просмотр коллекции рецептов
- Детальная информация о каждом рецепте
- Фильтрация по типу диеты, аллергенам, типу приема пищи
- Информация о калориях, макронутриентах, времени приготовления

### Генератор меню
- **Умный алгоритм генерации** с двухэтапным поиском:
  - Приоритизация топ-N рецептов по пригодности
  - Перебор всех комбинаций лучших рецептов
  - Fallback на случайный поиск при необходимости
- Автоматическая генерация ежедневного меню на основе:
  - **Целевых калорий** (автоматически из целей пользователя или ±30% допуск)
  - Максимального времени приготовления (если указано > 0)
  - Типа диеты (вегетарианская, веганская, без глютена)
  - Аллергенов
  - Наличия ингредиентов в кладовой
- **Генерация недельного меню** (7 дней):
  - Учет количества взрослых и детей
  - Автоматический расчет порций (взрослые + дети × 0.7)
  - Распределение калорий: завтрак 25%, обед 40%, ужин 35%
  - Анти-повторная логика (избегание повторений в течение 3 дней)
  - Умная балансировка макронутриентов (БЖУ) на всю неделю
  - Оптимизация PFC (белки, жиры, углеводы) с коррекцией отклонений
- **Улучшенная система скоринга**:
  - Калории (40% веса) - приоритет точного попадания
  - Время (25% веса) - соответствие ограничениям
  - Кладовая (20% веса) - использование имеющихся ингредиентов
  - Макроэлементы (10% веса) - баланс БЖУ (30/30/40)
  - Разнообразие (5% веса) - бонус за разные рецепты
- Улучшенная нормализация ингредиентов для точного сопоставления
- Учет предпочтений по кладовой (игнорировать/предпочитать/строго)
- **Управление меню**:
  - Сохранение дневных и недельных меню
  - Просмотр всех сохраненных меню
  - Удаление меню

### Управление целями
- Установка целевых калорий
- Настройка соотношения макронутриентов (белки, жиры, углеводы)
- Сохранение и отслеживание прогресса

### Кладовая
- Добавление и удаление ингредиентов
- Отслеживание количества продуктов
- Интеграция с генератором меню
- Автоматическое добавление базовых продуктов при создании пользователя

### Списки покупок
- Автоматическая генерация списков покупок для меню
- Учет ингредиентов из кладовой
- Отображение только недостающих продуктов
- Копирование списка в буфер обмена

### Авторизация
- **Полноценная система авторизации**:
  - Регистрация с email и паролем
  - Вход в систему
  - Управление профилем (имя, фамилия)
  - Смена пароля
  - Безопасное хранение паролей (bcrypt)
- **Telegram WebApp интеграция**:
  - Автоматическая аутентификация через Telegram
  - Поддержка обоих методов авторизации
- Адаптивный интерфейс для мобильных устройств

### Пользовательский интерфейс
- **Современный дизайн**:
  - Карточки с кнопками, прижатыми к низу
  - Единообразный стиль на всех страницах
  - Адаптивная верстка для всех устройств
  - **Переключение темы**: Светлая и темная тема с поддержкой системных настроек
- **Удобная навигация**:
  - Табы для переключения между дневными и недельными меню
  - Быстрый доступ к основным функциям с главной страницы
  - Кнопка переключения темы в навигации (десктоп и мобильная версия)
- **Визуализация данных**:
  - Круговая диаграмма БЖУ (белки, жиры, углеводы) на страницах меню
  - Отображение макронутриентов в процентах и граммах
  - Сумма калорий под диаграммой для лучшей читаемости

## 🏗 Архитектура проекта

Проект следует принципам Clean Architecture:

```
MyPlate/
├── frontend/                    # Next.js приложение
│   ├── app/                    # App Router страницы
│   │   ├── page.tsx           # Главная страница
│   │   ├── recipes/           # Страницы рецептов
│   │   ├── menu/              # Страницы меню
│   │   ├── menus/             # Список меню
│   │   ├── goals/             # Цели пользователя
│   │   ├── pantry/            # Кладовая
│   │   └── shopping-list/     # Списки покупок
│   ├── components/            # React компоненты
│   │   ├── ui/                # UI компоненты (shadcn/ui)
│   │   ├── nav.tsx            # Навигация
│   │   ├── macros-chart.tsx   # Круговая диаграмма БЖУ
│   │   ├── theme-provider.tsx # Провайдер темы
│   │   ├── theme-toggle.tsx   # Переключатель темы
│   │   └── telegram-provider.tsx # Telegram интеграция
│   └── lib/                   # Утилиты
│       ├── api.ts             # Axios конфигурация
│       └── utils.ts           # Вспомогательные функции
│
├── backend/                    # Go API сервер
│   ├── cmd/api/               # Точка входа приложения
│   │   └── main.go            # Инициализация и запуск сервера
│   ├── internal/              # Внутренние пакеты
│   │   ├── handlers/          # HTTP обработчики (контроллеры)
│   │   │   ├── auth_handler.go
│   │   │   ├── menu_handler.go
│   │   │   ├── recipe_handler.go
│   │   │   ├── user_handler.go
│   │   │   ├── pantry_handler.go
│   │   │   └── shopping_list_handler.go
│   │   ├── services/          # Бизнес-логика
│   │   │   ├── auth_service.go
│   │   │   ├── menu_service.go
│   │   │   ├── recipe_service.go
│   │   │   ├── user_service.go
│   │   │   ├── pantry_service.go
│   │   │   └── shopping_list_service.go
│   │   ├── repositories/      # Доступ к данным
│   │   │   ├── user_repository.go
│   │   │   ├── recipe_repository.go
│   │   │   ├── menu_repository.go
│   │   │   ├── pantry_repository.go
│   │   │   ├── goals_repository.go
│   │   │   └── shopping_list_repository.go
│   │   ├── models/            # Доменные модели
│   │   │   ├── user.go
│   │   │   ├── recipe.go
│   │   │   ├── menu.go
│   │   │   ├── pantry.go
│   │   │   └── shopping_list.go
│   │   └── middleware/        # Middleware
│   │       └── auth_middleware.go
│   └── pkg/                   # Публичные пакеты
│       └── database/          # Подключение к БД
│           └── database.go
│
├── sql/                       # SQL скрипты
│   ├── migrations/           # Миграции базы данных
│   │   └── 001_initial_schema.sql
│   └── seed.sql              # Тестовые данные
│
├── docker-compose.yml         # Конфигурация Docker Compose
├── Dockerfile.backend         # Dockerfile для backend
├── Dockerfile.frontend        # Dockerfile для frontend
├── Makefile                   # Команды для сборки и запуска
└── README.md                  # Документация
```

### Слои архитектуры

1. **Handlers (HTTP слой)**
   - Обработка HTTP запросов
   - Валидация входных данных
   - Формирование HTTP ответов
   - Обработка ошибок

2. **Services (Бизнес-логика)**
   - Основная логика приложения
   - Генерация меню
   - Расчет списков покупок
   - Валидация данных

3. **Repositories (Слой данных)**
   - Взаимодействие с базой данных
   - SQL запросы
   - Маппинг данных

4. **Models (Доменные модели)**
   - Структуры данных
   - JSON сериализация
   - Валидация

## 🚀 Быстрый старт

### Предварительные требования

- **Docker** и **Docker Compose** (рекомендуется)
- **Make** (опционально, но рекомендуется)
- Или вручную: Go 1.22+, Node.js 20+, PostgreSQL 15+

### Запуск через Docker (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd MyPlate
   ```

2. **Запустите все сервисы:**
   ```bash
   make up
   # или
   docker-compose up -d
   ```

3. **Приложение будет доступно:**
   - Frontend: http://localhost:3000
   - Backend API: http://localhost:8080
   - PgAdmin: http://localhost:5050 (admin@myplateservice.com / admin)

4. **Остановка сервисов:**
   ```bash
   make down
   # или
   docker-compose down
   ```

### Запуск вручную (без Docker)

#### 1. Настройка базы данных

```bash
# Установите PostgreSQL 15
# Создайте базу данных
createdb myplate
createdb myplate_test

# Примените миграции
psql -U postgres -d myplate -f sql/migrations/001_initial_schema.sql

# Загрузите тестовые данные
psql -U postgres -d myplate -f sql/seed.sql
```

#### 2. Настройка Backend

```bash
cd backend

# Установите зависимости
go mod download

# Создайте .env файл
cat > .env << EOF
DATABASE_URL=postgres://myplate:myplate@localhost:5432/myplate?sslmode=disable
JWT_SECRET=your-secret-key-change-in-production
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
PORT=8080
EOF

# Запустите сервер
go run cmd/api/main.go
```

#### 3. Настройка Frontend

```bash
cd frontend

# Установите зависимости
npm install

# Создайте .env.local файл
cat > .env.local << EOF
NEXT_PUBLIC_API_URL=http://localhost:8080
EOF

# Запустите dev сервер
npm run dev
```

## ⚙️ Установка и настройка

### Переменные окружения

#### Backend (`backend/.env`)

```env
# Подключение к базе данных
DATABASE_URL=postgres://myplateservice:myplateservice@postgres:5432/myplateservice?sslmode=disable

# Секретный ключ для JWT токенов (обязательно измените в продакшене!)
JWT_SECRET=your-secret-key-change-in-production

# Токен Telegram бота (для валидации initData, опционально)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Порт для API сервера
PORT=8080
```

#### Frontend (`frontend/.env.local`)

```env
# URL backend API
NEXT_PUBLIC_API_URL=http://localhost:8080

# Имя Telegram бота (опционально)
NEXT_PUBLIC_TELEGRAM_BOT_NAME=your-bot-name
```

#### Docker Compose

Переменные окружения для Docker Compose настроены в `docker-compose.yml`. Для изменения используйте файл `.env` в корне проекта или измените значения напрямую в `docker-compose.yml`.

### Настройка Telegram WebApp

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Установите `TELEGRAM_BOT_TOKEN` в переменные окружения backend
4. Настройте WebApp URL в настройках бота

### Тестовая авторизация

Для разработки доступен endpoint `/auth/test`, который создает тестового пользователя и возвращает JWT токен. Frontend автоматически использует его, если не запущен в Telegram WebApp.

## 📡 API документация

### Базовый URL

```
http://localhost:8080
```

### Аутентификация

Большинство endpoints требуют JWT токен в заголовке:

```
Authorization: Bearer <token>
```

#### Публичные endpoints

- `GET /health` - Проверка работоспособности сервера
- `GET /recipes` - Список всех рецептов
- `GET /recipes/:id` - Детали рецепта
- `POST /auth/telegram` - Аутентификация через Telegram
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему
- `GET /auth/test` - Тестовая аутентизация (для разработки)

#### Защищенные endpoints

Все остальные endpoints требуют JWT токен.

### Endpoints

#### Аутентификация

##### `POST /auth/telegram`

Аутентификация через Telegram WebApp initData.

**Request:**
```json
{
  "init_data": "query_id=AAHdF6IQAAAAAN0XohDhrOr6&user=%7B%22id%22%3A279058397%2C%22first_name%22%3A%22Vladislav%22%2C%22last_name%22%3A%22Kibenko%22%2C%22username%22%3A%22vdkfrost%22%2C%22language_code%22%3A%22ru%22%7D&auth_date=1662771648&hash=c501b8e33a..."
}
```

**Response:**
```json
{
  "user": {
    "id": 1,
    "telegram_id": 123456789,
    "username": "testuser",
    "first_name": "Test",
    "last_name": "User"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

##### `POST /auth/register`

Регистрация нового пользователя с email и паролем.

**Request:**
```json
{
  "email": "user@example.com",
  "password": "securepassword123",
  "first_name": "Иван",
  "last_name": "Иванов"
}
```

**Response:**
```json
{
  "user": {
    "id": 1,
    "email": "user@example.com",
    "first_name": "Иван",
    "last_name": "Иванов"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

##### `POST /auth/login`

Вход в систему с email и паролем.

**Request:**
```json
{
  "email": "user@example.com",
  "password": "securepassword123"
}
```

**Response:**
```json
{
  "user": {
    "id": 1,
    "email": "user@example.com",
    "first_name": "Иван",
    "last_name": "Иванов"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

##### `GET /auth/test`

Тестовая авторизация для разработки. Создает тестового пользователя и возвращает токен.

**Response:**
```json
{
  "user": {
    "id": 1,
    "telegram_id": 123456789,
    "username": "testuser",
    "first_name": "Test",
    "last_name": "User"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

##### `GET /auth/profile`

Получить профиль текущего пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
{
  "id": 1,
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "created_at": "2025-12-02T10:00:00Z"
}
```

##### `PUT /auth/profile`

Обновить профиль пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "first_name": "Петр",
  "last_name": "Петров"
}
```

**Response:**
```json
{
  "id": 1,
  "email": "user@example.com",
  "first_name": "Петр",
  "last_name": "Петров"
}
```

##### `PUT /auth/password`

Изменить пароль пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "old_password": "oldpassword123",
  "new_password": "newpassword456"
}
```

**Response:** 200 OK

#### Рецепты

##### `GET /recipes`

Получить список всех рецептов.

**Response:**
```json
[
  {
    "id": 1,
    "name": "Яичница с тостом",
    "description": "Классический завтрак...",
    "calories": 350,
    "proteins": 20.0,
    "fats": 18.0,
    "carbs": 28.0,
    "cooking_time": 10,
    "servings": 1,
    "meal_type": "breakfast",
    "diet_type": ["vegetarian"],
    "allergens": ["eggs", "dairy"],
    "ingredients": [
      {
        "name": "Яйца",
        "quantity": 2,
        "unit": "шт"
      }
    ],
    "instructions": ["Разбейте яйца..."]
  }
]
```

##### `GET /recipes/:id`

Получить детали рецепта по ID.

**Response:** Аналогично элементу из списка рецептов.

#### Меню

##### `POST /menus/generate`

Сгенерировать ежедневное меню.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "user_id": 1,
  "target_calories": 2000,
  "diet_type": "vegetarian",
  "allergies": ["nuts"],
  "max_total_time": 60,
  "consider_pantry": true,
  "pantry_importance": "prefer"
}
```

**Параметры:**
- `user_id` (int, обязательный) - ID пользователя (автоматически берется из токена на фронтенде)
- `target_calories` (int, опционально) - Целевые калории (если не указано, берется из user_goals)
- `diet_type` (string, опционально) - Тип диеты: "vegetarian", "vegan", "gluten-free"
- `allergies` (array, опционально) - Список аллергенов для исключения
- `max_total_time` (int, опционально) - Максимальное время приготовления в минутах (0 = не учитывать)
- `max_time_per_meal` (int, опционально) - Максимальное время на одно блюдо в минутах
- `consider_pantry` (bool) - Учитывать кладовую
- `pantry_importance` (string) - Важность кладовой: "ignore", "prefer", "strict"

**Response:**
```json
{
  "id": 1,
  "user_id": 1,
  "date": "2025-12-02T10:00:00Z",
  "total_calories": 1950,
  "total_time": 45,
  "meals": [
    {
      "recipe_id": 1,
      "meal_type": "breakfast",
      "calories": 350,
      "time": 10
    },
    {
      "recipe_id": 2,
      "meal_type": "lunch",
      "calories": 800,
      "time": 20
    },
    {
      "recipe_id": 3,
      "meal_type": "dinner",
      "calories": 800,
      "time": 15
    }
  ],
  "ingredients_used": [...],
  "missing_ingredients": [...]
}
```

##### `GET /menus`

Получить список всех меню пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
[
  {
    "id": 1,
    "user_id": 1,
    "date": "2025-12-02T10:00:00Z",
    "total_calories": 1950,
    "total_time": 45,
    "meals": [...]
  }
]
```

##### `GET /menus/:id`

Получить детали меню по ID.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:** Аналогично элементу из списка меню.

##### `GET /menus/daily`

Получить меню на конкретную дату.

**Headers:**
```
Authorization: Bearer <token>
```

**Query параметры:**
- `date` (string, опционально) - Дата в формате YYYY-MM-DD (по умолчанию сегодня)

**Response:** Объект меню или null, если меню не найдено.

##### `GET /menu/weekly`

Сгенерировать недельное меню (7 дней).

**Headers:**
```
Authorization: Bearer <token>
```

**Query параметры:**
- `adults` (int, опционально) - Количество взрослых (по умолчанию 1)
- `children` (int, опционально) - Количество детей (по умолчанию 0)
- `max_total_time` (int, опционально) - Максимальное время приготовления в минутах (0 = не учитывать)
- `max_time_per_meal` (int, опционально) - Максимальное время на одно блюдо в минутах (0 = не учитывать)
- `diet_type` (string, опционально) - Тип диеты (vegetarian, vegan, gluten-free)
- `allergies` (array, опционально) - Массив аллергенов
- `consider_pantry` (bool, опционально) - Учитывать кладовую (по умолчанию true)
- `pantry_importance` (string, опционально) - Важность кладовой (ignore, prefer, strict)

**Response:**
```json
{
  "week": [
    {
      "day": 1,
      "breakfast": {...},
      "lunch": {...},
      "dinner": {...},
      "totalCalories": 2560,
      "totalProteins": 104,
      "totalFats": 96,
      "totalCarbs": 336,
      "totalTime": 55
    },
    ...
  ]
}
```

##### `POST /menu/weekly/save`

Сохранить сгенерированное недельное меню в базу данных.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:** Объект WeeklyMenu (результат генерации)

**Response:**
```json
{
  "message": "Недельное меню успешно сохранено"
}
```

##### `GET /menus/weekly`

Получить все сохраненные недельные меню пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:** Массив недельных меню.

##### `DELETE /menus/:id`

Удалить меню по ID. Удаление возможно только для меню текущего пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
{
  "message": "Меню успешно удалено"
}
```

**Ошибки:**
- `404` - Меню не найдено или не принадлежит пользователю
- `500` - Внутренняя ошибка сервера

#### Цели пользователя

##### `POST /users/goals`

Установить цели пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "daily_calories": 2000,
  "protein_ratio": 30.0,
  "fat_ratio": 30.0,
  "carb_ratio": 40.0
}
```

**Response:**
```json
{
  "id": 1,
  "user_id": 1,
  "daily_calories": 2000,
  "protein_ratio": 30.0,
  "fat_ratio": 30.0,
  "carb_ratio": 40.0
}
```

##### `GET /users/goals`

Получить цели пользователя.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:** Объект целей или null, если цели не установлены.

#### Кладовая

##### `GET /pantry`

Получить список продуктов в кладовой.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
[
  {
    "id": 1,
    "user_id": 1,
    "name": "Яйца",
    "quantity": 12,
    "unit": "шт"
  }
]
```

##### `POST /pantry`

Добавить продукт в кладовую.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "name": "Молоко",
  "quantity": 500,
  "unit": "мл"
}
```

**Response:**
```json
{
  "id": 2,
  "user_id": 1,
  "name": "Молоко",
  "quantity": 500,
  "unit": "мл",
  "created_at": "2025-12-02T10:00:00Z",
  "updated_at": "2025-12-02T10:00:00Z"
}
```

##### `DELETE /pantry/:id`

Удалить продукт из кладовой.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:** 204 No Content

#### Списки покупок

##### `GET /shopping-list/:menu_id`

Получить список покупок для меню.

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
{
  "id": 1,
  "user_id": 1,
  "menu_id": 1,
  "items": [
    {
      "name": "Помидоры",
      "quantity": 200,
      "unit": "г",
      "reason": ["lunch", "dinner"]
    }
  ],
  "created_at": "2025-12-02T10:00:00Z",
  "updated_at": "2025-12-02T10:00:00Z"
}
```

### Коды ошибок

- `200 OK` - Успешный запрос
- `201 Created` - Ресурс создан
- `204 No Content` - Успешное удаление
- `400 Bad Request` - Неверный формат запроса
- `401 Unauthorized` - Требуется авторизация или неверный токен
- `404 Not Found` - Ресурс не найден
- `500 Internal Server Error` - Внутренняя ошибка сервера

## 🗄 Структура базы данных

### Таблица `users`

Хранит информацию о пользователях.

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| telegram_id | BIGINT UNIQUE | ID пользователя в Telegram (NULL для email/password) |
| email | TEXT UNIQUE | Email пользователя (NULL для Telegram) |
| password_hash | TEXT | Хеш пароля (bcrypt) |
| username | TEXT | Имя пользователя |
| first_name | TEXT | Имя |
| last_name | TEXT | Фамилия |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

**Ограничения:**
- `users_telegram_or_email` - Должен быть указан либо `telegram_id`, либо `email`

### Таблица `user_goals`

Цели пользователя по калориям и макронутриентам.

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| user_id | INT | ID пользователя (FK) |
| daily_calories | INT | Целевые калории в день |
| target_proteins | NUMERIC(10,2) | Целевое количество белков (г) |
| target_fats | NUMERIC(10,2) | Целевое количество жиров (г) |
| target_carbs | NUMERIC(10,2) | Целевое количество углеводов (г) |
| protein_ratio | NUMERIC(5,2) | Процент белков |
| fat_ratio | NUMERIC(5,2) | Процент жиров |
| carb_ratio | NUMERIC(5,2) | Процент углеводов |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Таблица `recipes`

Рецепты блюд.

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| name | TEXT | Название рецепта |
| description | TEXT | Описание |
| calories | INT | Калории |
| proteins | NUMERIC(10,2) | Белки (г) |
| fats | NUMERIC(10,2) | Жиры (г) |
| carbs | NUMERIC(10,2) | Углеводы (г) |
| cooking_time | INT | Время приготовления (минуты) |
| servings | INT | Количество порций |
| meal_type | TEXT | Тип приема пищи: breakfast, lunch, dinner, snack |
| diet_type | TEXT[] | Типы диет: vegetarian, vegan, gluten-free |
| allergens | TEXT[] | Аллергены: eggs, dairy, nuts, fish, gluten |
| ingredients | JSONB | Массив ингредиентов: `[{"name": "...", "quantity": 100, "unit": "г"}]` |
| instructions | TEXT[] | Массив инструкций по приготовлению |
| image_url | TEXT | URL изображения (опционально) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Таблица `pantry_items`

Продукты в кладовой пользователя.

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| user_id | INT | ID пользователя (FK) |
| name | TEXT | Название продукта |
| quantity | NUMERIC(10,2) | Количество |
| unit | TEXT | Единица измерения |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Таблица `menus`

Сгенерированные ежедневные меню.

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| user_id | INT | ID пользователя (FK) |
| date | DATE | Дата меню |
| total_calories | INT | Общие калории |
| total_time | INT | Общее время приготовления (минуты) |
| meals | JSONB | Массив блюд: `[{"recipe_id": 1, "meal_type": "breakfast", "calories": 350, "time": 10}]` |
| ingredients_used | JSONB | Использованные ингредиенты из кладовой |
| missing_ingredients | JSONB | Недостающие ингредиенты |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Таблица `shopping_lists`

Списки покупок для меню.

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| user_id | INT | ID пользователя (FK) |
| menu_id | INT | ID меню (FK) |
| items | JSONB | Массив товаров: `[{"name": "...", "quantity": 100, "unit": "г", "reason": ["breakfast", "lunch"]}]` |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Индексы

- `idx_users_telegram_id` - Быстрый поиск по telegram_id
- `idx_recipes_meal_type` - Фильтрация по типу приема пищи
- `idx_recipes_calories` - Фильтрация по калориям
- `idx_recipes_cooking_time` - Фильтрация по времени
- `idx_recipes_diet_type` - GIN индекс для массива типов диет
- `idx_recipes_allergens` - GIN индекс для массива аллергенов
- `idx_recipes_ingredients` - GIN индекс для JSONB ингредиентов

## 💻 Разработка

### Команды Makefile

```bash
# Запуск всех сервисов
make up

# Остановка всех сервисов
make down

# Запуск backend локально (без Docker)
make api

# Запуск frontend локально (без Docker)
make next

# Сборка backend
make build-api

# Сборка frontend
make build-frontend

# Очистка артефактов сборки
make clean

# Установка зависимостей frontend
make install-frontend

# Установка зависимостей backend
make install-backend

# Запуск тестов
make test

# Форматирование кода
make fmt
```

### Разработка Backend

```bash
cd backend

# Установка зависимостей
go mod download

# Запуск с hot reload (требует установки air или аналогичного инструмента)
air

# Или обычный запуск
go run cmd/api/main.go

# Запуск тестов
go test ./...

# Форматирование кода
go fmt ./...

# Линтинг
golangci-lint run
```

### Разработка Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint

# Проверка типов
npm run type-check
```

### Миграции базы данных

Миграции находятся в `sql/migrations/` и применяются автоматически при первом запуске PostgreSQL контейнера через `docker-entrypoint-initdb.d`.

Для ручного применения:

```bash
# Через Docker
docker exec -i myplateservice-postgres psql -U myplateservice -d myplateservice < sql/migrations/001_initial_schema.sql
docker exec -i myplateservice-postgres psql -U myplateservice -d myplateservice < sql/migrations/002_add_email_password.sql

# Или напрямую
psql -U myplateservice -d myplateservice -f sql/migrations/001_initial_schema.sql
psql -U myplateservice -d myplateservice -f sql/migrations/002_add_email_password.sql
```

### Тестовые данные

Тестовые данные находятся в `sql/seed.sql` и включают:
- 2 тестовых пользователя
- 10 рецептов с полной информацией
- 41 продукт в кладовой для первого пользователя
- Примеры целей пользователя

Для загрузки тестовых данных:

```bash
# Через Docker
docker exec -i myplateservice-postgres psql -U myplateservice -d myplateservice < sql/seed.sql

# Или напрямую
psql -U myplateservice -d myplateservice -f sql/seed.sql
```

### Структура кода Backend

#### Handlers

HTTP обработчики принимают запросы, валидируют данные и вызывают сервисы.

Пример:
```go
func (h *MenuHandler) Generate(c *fiber.Ctx) error {
    var req models.MenuGenerateRequest
    if err := c.BodyParser(&req); err != nil {
        return c.Status(400).JSON(fiber.Map{"error": "Неверное тело запроса"})
    }
    
    menu, err := h.menuService.GenerateMenu(&req)
    if err != nil {
        return c.Status(500).JSON(fiber.Map{"error": err.Error()})
    }
    
    return c.JSON(menu)
}
```

#### Services

Бизнес-логика приложения. Сервисы используют репозитории для доступа к данным.

Пример:
```go
func (s *MenuService) GenerateMenu(req *models.MenuGenerateRequest) (*models.Menu, error) {
    // Получение рецептов
    recipes, err := s.recipeRepo.GetFiltered(...)
    
    // Генерация меню
    bestMenu := s.findBestMenuCombination(...)
    
    // Сохранение
    err = s.menuRepo.Create(bestMenu)
    
    return bestMenu, nil
}
```

#### Repositories

Доступ к базе данных. Используют `database/sql` для выполнения SQL запросов.

Пример:
```go
func (r *RecipeRepository) GetAll() ([]models.Recipe, error) {
    query := `SELECT id, name, ... FROM recipes ORDER BY name`
    rows, err := database.DB.Query(query)
    // ... обработка результатов
}
```

### Алгоритм генерации меню

Улучшенный двухэтапный алгоритм с умным поиском:

#### Этап 1: Подготовка данных
1. **Получение целевых калорий**:
   - Если не указаны в запросе, автоматически берутся из `user_goals`
   - Если целей нет, используется значение по умолчанию (2000 ккал)
2. **Фильтрация рецептов** по типу диеты, аллергенам, типу приема пищи, времени
3. **Оценка рецептов** на основе наличия ингредиентов в кладовой
4. **Сортировка рецептов** по пригодности (fitness score):
   - Кладовая (40%) - наличие ингредиентов
   - Время (30%) - соответствие ограничениям
   - Калории (30%) - близость к целевым калориям на прием пищи

#### Этап 2: Генерация комбинаций

**Стратегия 1: Умный поиск (приоритет)**
- Берется топ-N рецептов каждого типа (до 15 лучших)
- Перебираются **все комбинации** топ рецептов
- Для каждой комбинации вычисляется score с учетом:
  - **Калории (40%)** - приоритет точного попадания в целевые калории
  - **Время (25%)** - соответствие ограничениям по времени
  - **Кладовая (20%)** - использование ингредиентов из кладовой
  - **Макроэлементы (10%)** - баланс БЖУ (идеальное соотношение 30/30/40)
  - **Разнообразие (5%)** - бонус за разные рецепты
- Бонусы и штрафы:
  - +20% бонус за точное попадание в целевые калории
  - Штраф до 70% за отклонение калорий >30%
  - Штраф до 50% за превышение времени

**Стратегия 2: Случайный поиск (fallback)**
- Если не найдено хорошее меню (score < 0.3), используется случайный поиск
- До 1000 попыток случайных комбинаций
- Применяются те же правила скоринга и штрафов

#### Этап 3: Сохранение и генерация списка покупок
1. **Сохранение меню** в базу данных с правильным `user_id`
2. **Расчет ингредиентов** - какие есть в кладовой, какие нужно купить
3. **Генерация списка покупок** на основе недостающих ингредиентов
4. **Сохранение списка покупок** с привязкой к меню

#### Особенности алгоритма:
- **Улучшенная нормализация ингредиентов** - точное сопоставление названий (lowercase, удаление лишних пробелов)
- **Приоритизация целевых калорий** - меню максимально близко к целевым калориям
- **Учет макроэлементов** - баланс белков, жиров и углеводов
- **Разнообразие** - избегание повторений рецептов
- **Надежность** - всегда возвращает лучшее найденное меню, даже при строгих ограничениях

## 🚢 Деплоймент

### Production настройки

1. **Измените секретные ключи:**
   ```env
   JWT_SECRET=<сильный-случайный-ключ>
   TELEGRAM_BOT_TOKEN=<ваш-токен>
   ```

2. **Настройте базу данных:**
   - Используйте managed PostgreSQL (AWS RDS, Google Cloud SQL, etc.)
   - Настройте резервное копирование
   - Используйте connection pooling

3. **Настройте reverse proxy:**
   - Nginx или Caddy для frontend
   - Load balancer для backend (если нужно)

4. **Настройте мониторинг:**
   - Логирование (ELK, Loki)
   - Метрики (Prometheus, Grafana)
   - Алерты

### Docker Compose для Production

Создайте `docker-compose.prod.yml`:

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  api:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      PORT: 8080
    depends_on:
      - postgres
    restart: always

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    environment:
      NEXT_PUBLIC_API_URL: ${API_URL}
    depends_on:
      - api
    restart: always

volumes:
  postgres_data:
```

### CI/CD

Пример GitHub Actions workflow:

```yaml
name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and push Docker images
        run: |
          docker-compose build
          # Push to registry
```

## 📝 Лицензия

MIT

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker logs myplateservice-api` и `docker logs myplateservice-frontend`
2. Убедитесь, что все переменные окружения установлены (см. `.env.example`)
3. Проверьте подключение к базе данных
4. Убедитесь, что применены все миграции (включая `002_add_email_password.sql`)
5. Создайте issue в репозитории

## 🎯 Дальнейшее развитие

Возможные улучшения:
- [x] Полноценная система авторизации (email/password)
- [x] Улучшенный алгоритм генерации меню
- [x] Автоматическое использование целевых калорий из целей пользователя
- [x] Учет макроэлементов в скоринге меню
- [x] Улучшенная нормализация ингредиентов
- [ ] Добавление изображений рецептов
- [ ] Экспорт меню в PDF
- [ ] Интеграция с календарем
- [ ] Уведомления о меню
- [ ] История меню и статистика
- [ ] Рекомендации на основе предпочтений
- [ ] Мультиязычность (i18n)
- [ ] Темная тема
- [ ] Мобильное приложение
